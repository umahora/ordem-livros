<script lang="ts">
    export let serie: any = null;
</script>

{#if serie}
    <a 
        href="/series/{serie.id}"
        class="flex flex-col rounded-md overflow-hidden border border-zinc-800"
    >
        <img 
            class="h-96 object-cover"
            src={serie.imagem} alt="Imagem da seria {serie.titulo}"
        />
        <div class="flex flex-col gap-2 p-5">
            <span class="text-2xl">{serie.titulo}</span>
            <span class="text-sm text-zinc-400 text-justify">{serie.descricao}</span>
            <button class="botao azul">Leia Mais</button>
        </div>
    </a>
{/if}