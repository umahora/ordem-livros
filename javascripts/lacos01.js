const pares = [0,2,4,6,8,10,12,14,16]

let novo = pares.map(elemento => elemento * 2)

pares.forEach(elemento => console.log(elemento * 2))

console.log(novo)
                                             